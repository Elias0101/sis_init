// Log in to my server
// Then log in to my system as the root user:
ssh root@server_ip_address

// Create a new user account using the adduser command.
adduser username

// Add password

// Add the new user to the sudo group

su - username